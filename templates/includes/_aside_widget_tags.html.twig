<div class="aside-widget">
    <div class="tags-widget">
        <ul>
            {% for tag in tags %}
                <li>
                    <a href="#">{{ tag.title }}</a>
                </li>
            {% endfor %}
        </ul>
    </div>
</div>
